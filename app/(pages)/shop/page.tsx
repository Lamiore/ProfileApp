import ShopPlaceholder from "@/features/shop/components/ShopPlaceholder";

export default function ShopPage() {
    return (
        <div
            style={{
                margin: "0 auto",
                padding: "80px clamp(1rem, 2.6vw, 2.5rem) 64px",
            }}
        >
            <ShopPlaceholder />
        </div>
    );
}
