import Blog from "@/components/sections/blog";

export default function BlogPage() {
    return (
        <div style={{ maxWidth: "960px", margin: "0 auto", padding: "80px clamp(1rem, 2.6vw, 2.5rem) 64px" }}>
            <Blog />
        </div>
    );
}
