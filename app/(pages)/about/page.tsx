import AboutWindow from "@/components/windows/aboutwindow";

export default function AboutPage() {
    return (
        <div style={{ maxWidth: "680px", margin: "0 auto", padding: "32px clamp(1rem, 2.6vw, 2.5rem) 64px" }}>
            <AboutWindow />
        </div>
    );
}
