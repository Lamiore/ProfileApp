import GalleryWindow from "@/components/windows/gallerywindow";

export default function GalleryPage() {
    return (
        <div style={{ maxWidth: "1200px", margin: "0 auto", padding: "32px clamp(1rem, 2.6vw, 2.5rem) 64px" }}>
            <GalleryWindow />
        </div>
    );
}
