import Gallery from "@/features/gallery/components/GalleryWindow";

export default function GalleryPage() {
    return (
        <div style={{ margin: "0 auto", padding: "80px clamp(1rem, 2.6vw, 2.5rem) 64px" }}>
            <Gallery />
        </div>
    );
}
